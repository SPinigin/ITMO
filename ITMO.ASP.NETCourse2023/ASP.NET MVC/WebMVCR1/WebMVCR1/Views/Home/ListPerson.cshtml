@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model WebMVCR1.Models.PersonRepository

@{
    // Layout = "~/Views/_BasicLayout.cshtml";
    ViewBag.Title = "Список участников";
}

<div>
    <h3>Список участников</h3>
        @* <p>Персон всего @Model.NumberOfPerson</p>

            <ul>
               @foreach (var d in Model.GetAllResponses)
               {
                     <li>@d.ToString()</li>
               }
            </ul> *@

      <p>Персон всего @ViewBag.Count</p> 
    
            <ul>
                @foreach (var c in ViewBag.Pers)
                {
                    <li>@c.ToString()</li>
                }
            </ul>

    <p>@Html.ActionLink("На главную", "Index")</p>
</div>